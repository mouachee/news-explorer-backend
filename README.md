# news-explorer-backend

## News Explorer Backend:

This backend project is focused on creating a server for the News Explorer application. It used MongoDB to store the databases and Postman to test the application.

## Running the Project

`npm run start` — to launch the server

`npm run dev` — to launch the server with the hot reload feature
